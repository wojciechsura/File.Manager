<UserControl x:Class="File.Manager.Controls.Pane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:File.Manager.BusinessLogic.ViewModels.Pane;assembly=File.Manager.BusinessLogic"
             xmlns:local="clr-namespace:File.Manager.Controls" 
             xmlns:res="clr-namespace:File.Manager.Resources.Controls.Pane;assembly=File.Manager.Resources"
             d:DataContext="{d:DesignInstance Type=vm:PaneViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" GotFocus="HandlePaneGotFocus" LostFocus="HandlePaneLostFocus"
             PreviewKeyDown="HandlePanePreviewKeyDown">
    <UserControl.Resources>
        <DataTemplate x:Key="NameColumnTemplate" DataType="{x:Type vm:ItemViewModel}">
            <DockPanel>
                <Image DockPanel.Dock="Left" Margin="{StaticResource DialogItemsMargin}" Width="16" Height="16" Source="{Binding SmallIcon}" />
                <TextBlock Margin="{StaticResource DialogItemsExceptLeftMargin}" Text="{Binding Name, Mode=OneWay}" />
            </DockPanel>
        </DataTemplate>

        <CollectionViewSource x:Key="cvsItems" Source="{Binding Items}" />
    </UserControl.Resources>
    <DockPanel>
        <ListView x:Name="lbItems" ItemsSource="{Binding Source={StaticResource cvsItems}}" SelectedItem="{Binding SelectedItem}" 
                  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" SelectionMode="Single" PreviewKeyDown="ItemDisplayPreviewKeyDown" 
                  MouseDoubleClick="HandleListViewMouseDoubleClick" IsSynchronizedWithCurrentItem="True" SelectionChanged="HandleListViewItemSelectionChanged"
                  GotFocus="HandleListViewGotFocus">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                            <Setter Property="TextBlock.Foreground" Value="{StaticResource SelectedItemForegroundBrush}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView />
            </ListView.View>
        </ListView>
    </DockPanel>
</UserControl>
